36
select device_id,
    age
from user_profile
order by age ;
37
select device_id,
    gpa,
    age
from user_profile
order by gpa asc,age asc;
38
select device_id,
    gpa,
    age
from user_profile
order by gpa desc,age desc;