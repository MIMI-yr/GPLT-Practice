# 2026-09-06 天梯赛刷题记录

## 今日完成

- L2-041
- L2-043

## 完成情况

- 状态：独立完成
- 用时：
- 提交次数：

## 解题思路

- L2-041 明确的优先级:
先看盒子栈顶能不能用
↓
能用：直接 pop 栈顶

不能用：
↓
再去推送器拿
↓
推送器拿到的能用：插到松枝
↓
不能用：push 进盒子
- L2-043
树上路径 + 动态增加目标点 + 统计最小遍历路程
与普通图里的最短路算法题不同，不是 Dijkstra、Floyd 那种
    树有一个特别好的性质：
任意两个点之间只有唯一一条路径。

2 × 当前必须经过的边数 - 当前最深送餐点的深度

## 错误记录

- 

## 今日知识点

- stack<int> box;== //C++ 帮你把这些东西全封装好了
    int st[1005];
    int top=-1;

    void pushStack(int x)
    {
        top++;
        st[top]=x;
    }

    int popStack()
    {
        int x=st[top];
        top--;
        return x;
    }
    stack<int> box;
最常用的就是这几个：
box.push(x);   // 压栈
box.pop();     // 弹出栈顶
box.top();     // 查看栈顶
box.empty();   // 判断栈是否为空
box.size();    // 栈里有几个元素

## 明日计划

-